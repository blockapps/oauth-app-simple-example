apiDebug: false
password: '1234'
timeout: 600000
libPath: ./server/lib
contractsPath: ./test/fixtures/

# WARNING - extra strict syntax
# DO NOT change the nodes order
# node 0 is the default url for all single node api calls
nodes:
  - id: 0
    stratoUrl: 'http://localhost/strato-api'
    blocUrl: 'http://localhost/bloc/v2.2'
    searchUrl: 'http://localhost/cirrus'

oauth:
  appTokenCookieName: "myapp_session"
  stratoUrl: "http://example.com"
  clientId: "oauth-client-id"
  clientSecret: "oauth-client-secret"
  paths:
    tokenHost: "https://login.microsoftonline.com"
    tokenPath: "/<azure-tenant-id>/oauth2/token"
    authorizePath: "/<azure-tenant-id>/oauth2/authorize"
  redirectUri: "http://localhost:3000/callback"